# 2026-08-20 — day 2

**Settled D7.** Adopted the audit's positioning: task-contract compiler, headline is the refusal —
"it refuses to dispatch a brief the recipient cannot execute". Dials are the IR, linter is the
product. README, roadmap and repo description updated. Name and API unchanged.

**Pre-registered the 20-task A/B** on pi. Four information-matched arms built from one Task object
so only rendering differs. Wrote the power analysis honestly: n=20 binary resolves 25–30pp, so cost
and step metrics carry the finding and pass rate is directional only.

**Fixed a confound I built in.** "Tier A vs Tier B" conflated capability with vendor — an
interaction could have been one company's post-training preferring checklists wearing the costume of
a capability law. Now a 2×2 of {small, large} × {two families}: row effect means capability, column
effect means the profiles are vendor lore.

**Family bias.** Judging is a two-family panel with published inter-rater agreement; self-preference
is measured by having each judge score its own family blind, and if that bias exceeds the effect
being claimed, the judged portion is void. Objective metrics decide 16 of 20 tasks so judgement
touches as little as possible.

**Arm M died.** Sent it for audit suspecting it was weak. It was worse than weak — a straw man the
product could not lose to (refuted/R1). Replaced with a one-dial ablation across the render
threshold, scored on scope violations and steps. Published as Amendment 1 rather than a quiet
repair.

**Process failure worth recording.** A reply arrived at the expected path, in the expected format,
with file:line citations — and it was an audit of MacroGaijin, because the receiving pane had other
work queued and applied my "write your reply here" instruction to whatever it finished next. Every
quality signal was present and the answer was to the wrong question (findings/F1). Fixed by naming
reply files after the task, not the agent, and by checking the pane's title before trusting a reply.
The user then opened a dedicated grok session for this project.

**Got a real test machine.** `operatorserver` came back up and is on the tailnet, direct path, same
LAN. i7-7700HQ, **30 GiB RAM** against this laptop's 7.4 GiB, 8 GiB of working swap, 71 GB free,
Docker 29.6. Provisioned it: docker group, Node 22.22.1 pinned for pi, repo cloned, venv, and the
full suite passes on **Python 3.14** in 0.180s versus 0.877s here. Containers run as the user
without sudo.

This changes the eval plan for the better. "Fresh session per run" and "hidden tests the agent must
not read" are guarantees a container gives and discipline does not, and 480 runs can go unattended
instead of occupying the daily machine. The GTX 1050's 4 GB is not an inference host, so the eval's
models still come from APIs — the server's value is RAM, isolation and parallelism.

**Filed H4**, a handoff hypothesis. The observation that makes it belong here: Bliss Point already
compiles the outbound direction, and every profile already carries a `return_contract` describing
what must come back. A deterministic handoff renderer is the inverse of the compiler over the same
data, not a new subsystem. The prediction is a split — structured state templates cleanly,
unstructured reasoning does not — and the useful finding would be *which* fields need a model.

**Built the flat renderer (arm F)** before the task corpus, which was the point — a control written
after you know the tasks is a control you can tune.

The design question that mattered: arm F emits *everything*, while arm C *selects* — low
`context_volume` drops evidence, low `decomposition` collapses the subtask list. So the control
carries **more** text than the thing it controls for, and that is correct rather than a flaw. The
comparison is now honest in the direction that hurts: if a shaped brief wins while saying less,
shaping did the work; if it does not, gathering the fields was the whole product.

Enforced it with tests instead of intent: `flat()`'s signature is asserted to take only a task, so
recipient-awareness cannot creep in; every populated field must appear; and arm C's emitted fields
must be a subset of arm F's across all seven profiles and all six phases. Added a fourth test
asserting the compiler really does drop fields somewhere, because if it never did, the superset
property would be vacuously true and the whole comparison meaningless.

41 tests. A two-minute CLI hang mid-build turned out to be the external drive stalling, not code.

## Corpus: the gate before the tasks

Built `bliss-point-corpus` — separate repo, because hidden acceptance tests cannot live where the
agent under test can read them, and because a corpus is evidence with its own provenance rules
rather than code.

Shipped empty on purpose. **0 of 20 tasks.** What exists is the machinery that decides what may
enter:

- `tools/corpus_check.py` — six admissibility rules, fails rather than warns.
- `tools/render_arms.py` — one Task to R/F/C/A plus a field manifest; refuses to emit an arm set
  where C or A carries a field arm F does not.
- `tools/verify_candidate.py` — a sourced URL to a verified provenance block, via the GitHub API.
- `tests/test_gate.py` — 17 tests, one deliberately broken fixture per rule.

A sixth rule appeared while building: **arm A must not be vacuous**. The gate compiles the task,
flips the named dial to its ablated value, and rejects the task if the rendered text does not
change. Without it a task can quietly contribute a copy of arm C to arm A, diluting the only
comparison that can kill a dial by name. This was not in the protocol; §4.1 now records it.

Rule 5's fixture immediately found a real hole in D4's gate — see
[F2](../findings/F2-the-gate-found-a-hole-in-the-gate.md). Library patched, 42 tests green.

**Sourcing dispatched to balthasar (deep research).** Corpus rule 4 forbids tasks authored while
looking at the dials, which makes sourcing a research job, not a writing job. Hard criteria sent:
permissive licence, offline pytest suite, under ~20k LOC, user-reported symptom rather than
maintainer TODO, fixing PR under 40 non-test lines across ≤3 files, and the PR must have added a
test. Four or more must be **vague** reports ("it's broken", maintainer asks for clarification) —
those become the two under-specified tasks, where confident code is the failure.

It went further than briefed: rather than summarising search results it wrote its own screener and
ran `gh` against candidate repos directly. Nothing it returns becomes provenance without
`verify_candidate.py` confirming it against the API first — smoke-tested against a real merged PR,
which it correctly rejected for touching no test file.

## Sourcing round 1: 31 verified, and a briefing defect

Balthasar hit its tool-iteration cap and handed off partial. What it had was good: 31 candidates
across 15 repos, screened end-to-end with `gh` rather than summarised from search results.

Re-verified every one independently with `verify_candidate.py` against the GitHub API:
**31 proposed, 31 confirmed, 0 rejected, 0 bad URLs.** Worth recording, since the verification rule
exists because of [F1](../findings/F1-agent-reports-need-grepping.md). Nothing to catch this time.
8 of 31 are flagged criterion 6 (filed by OWNER/MEMBER/COLLABORATOR, so possibly a maintainer TODO
rather than a user symptom); with 19 unflagged bug-fix candidates for 6 slots, they are simply not
needed.

Supply against quota: bugfix **27/6**, feature 3/5, refactor **0/3**, investigation 1/4,
under-specified **0/2**.

The three empties are my fault, not the researcher's — see
[F3](../findings/F3-one-rule-for-five-ground-truths.md). The brief applied one acceptance rule to
all five categories (*the fixing PR added a test*), which protocol §4 never said and which is
impossible for refactor, investigation and under-specified by construction. The gate carried the
identical error and would have rejected a correctly sourced refactor. Both fixed; gate is now
category-aware with four new tests, 21 green.

Round 2 dispatched with per-category criteria, plus an operational fix for the cap: write the
output file first and append each candidate as it is confirmed, so a cut-off preserves the work.
A correction is still owed on that brief — it told balthasar features were done, and they are 3/5.
Queued rather than sent as an interrupt.

## Harness bake-off pre-registered, and a third gap defect

Operator filed five claims: melchior codes better on DeepSeek Harness (H5) and on pi (H8),
balthasar researches better on Playwright CLI (H6) and on dsh (H9), casper is cheaper and sharper
on Prime agent (H7). Protocol pre-registered at [docs/harness-bakeoff.md](../../docs/harness-bakeoff.md).

H8 and H9 arrived after the first three and changed the design for the better: three isolated A/Bs
became a **role × harness grid**. Two roles across three harnesses, one harness (dsh) across two
roles, which buys an interaction the pairs could not see — dsh winning for both roles is a general
harness effect, dsh winning only for melchior is a **role × harness interaction**, which is this
project's own thesis one level up and the first evidence for it measured on something we did not
build.

The load-bearing control is the operator's own instruction: **all harnesses on MiniMax OAuth**.
Without it, "melchior codes better on dsh" measures DeepSeek's model and credits dsh's harness —
[R1](../refuted/R1-arm-M-as-falsifier.md) in a new costume. A harness that cannot hold the model
constant gets reported as *not tested*, never run with a footnote.

Phase 0 is a feasibility probe that installs nothing, dispatched to codex on a brief **compiled by
Bliss Point for codex**. Dogfooding it found [F4](../findings/F4-two-branches-each-assumed-the-other.md):
at `decomposition` 0.35 with `acceptance_binding` 0.9 — codex's designed shape — a Task carrying
subtasks rendered **no acceptance criteria at all, and raised no gap**. Both branches assumed the
other was carrying them. Every codex brief this project has compiled with subtasks on the Task has
been missing its definition of done. Fixed, regression test across all profiles, 42 green.

A second, milder instance surfaced immediately after: `escalation_explicitness` 0.45 dropped the
escalation triggers from the same brief. That drop is *correct* — selection is what the dial is for
— but the triggers here were safety stops (do not take pihole's port 53, do not fall back to the
7.4GiB laptop, do not copy an identity to the server). Overridden to 0.75 with the reason recorded.
**Open design question, not yet built:** a dial silently dropping a stop condition is doing its job
as specified, and the specification is wrong. The F4 invariant — no populated field leaves the Task
without appearing in the brief or raising a gap — would cover this too, and is now the next thing
to write.

## Sourcing closed: 45 candidates, and a rule the protocol was missing

Rounds 2 and 3 filled the four short categories. Totals: bugfix 27, feature 7, refactor 4,
investigation 4, under-specified 3 — **45 verified candidates for 20 slots**, every category
supplied.

Across the three rounds the tool caught 4 bad candidates and reading caught 4 more, which is the
number worth remembering: **mechanical admissibility is necessary and not sufficient**, and the
README now says so rather than implying the gate is the whole defence. The four the tool missed:
a behaviour-changing "refactor", a licence request filed as a vague bug, and two refactors that
were valid but degenerate.

Those last two produced **rule 7**. `psf/black` #5278 removes a duplicate list entry; #5277 removes
an unused constant. Real refactors, mechanically perfect, and useless — no brief format could
plausibly fail a one-line deletion, so all four arms pass and the task yields four identical
results. A **ceiling effect**, which the protocol had no rule against: rule 3 dropped tasks too hard
for the weak tier, and nothing dropped tasks too easy for any brief. That asymmetry is the more
dangerous one, because a ceiling effect burns identical budget while looking like a row of
successes.

Rule 7 reuses the arms already built: pilot **arm C** to confirm the task is not too hard, pilot
**arm R** — the floor — to confirm it is not too easy. `mixed` on the arm-R pilot is ideal. This is
the third protocol defect found by contact with real data rather than by reasoning about the design
([F3](../findings/F3-one-rule-for-five-ground-truths.md) was the second), and like the others it
was cheap to fix only because it was found before any runs were spent.

## Bake-off Phase 0: 0 GO / 5 NO-GO, and the grid was confounded anyway

Codex probed all five harnesses from source, installed nothing, and returned five NO-GOs. I
re-verified every decisive claim against primary source before accepting it: all five repos exist
with the stated licences; pi's `minimax.ts` really does carry only `auth: { apiKey: ... }` with no
`oauth` member and its oauth directory really has no minimax module; dsh's limitation is verbatim
(*"A provider that authenticates through OAuth alone is not offered"*); Prime's OAuth registry
really exports only Anthropic, GitHub Copilot and OpenAI Codex.

**H6 is permanently dead** — Playwright CLI is not an agent harness, it is browser automation driven
*by* an agent. A category error, killed for the price of one source probe
([R3](../refuted/R3-playwright-cli-as-a-research-harness.md)).

The bigger result is one codex reported in pieces without joining up
([F5](../findings/F5-related-provider-code-is-not-a-confound.md)): **pi, Prime Agent and dsh all descend
from or embed pi's provider layer.** Prime is a hard fork of pi-mono and says so; dsh's LLM seam is
literally named `dsh-llm-pi-ai`. So the five NO-GOs are one fact observed three times, and — worse
for the design — the grid was preparing to compare three forks of one codebase and call the
differences "harness". Third time this lab has assumed a contrast instead of establishing it, after
arm M and after tier-vs-family.

Phase 0 cost nothing and returned the seam report §8 said was the real deliverable. Phase 1 is not
authorised; the choice between stopping, paying for a metered API path, and writing a MiniMax OAuth
adapter is the operator's, and all three should now be priced against **one** contested cell rather
than five.

## Bake-off closed: stop and bank the seam report

Operator's call, and the right one. H6 stays **refuted**; H5/H7/H8/H9 are recorded as **NOT
TESTED**, which is deliberately not the same as refuted — no harness but Hermes implements MiniMax
OAuth, so the model could not be held constant and §2.1 forbids running a cell that cannot. They are
unfalsifiable as stated, not false, and the conditions that would reopen them are written down.

The [seam report](../../docs/harness-seams.md) is now a standalone deliverable rather than a
subsection: dsh is the adoption target if one is wanted (`complete: true` plus
`includeRuntimeContext: false` is a byte-identical prompt channel, and C1 already established the
plugin seam needs no core patch); pi stays the eval harness, now for a better reason than
multi-provider — its `--no-skills / --no-extensions / --no-context-files / --no-prompt-templates`
suppression flags are what make information-matched arms possible at all.

The line worth stealing from dsh's own docs: **a tool allowlist is visibility composition, not an
authority boundary.** This project has been loose about that distinction in its own language around
tool scoping.

F5 sent to codex for adversarial pair review, with the counter-argument I most want made spelled out
for it: a shared *provider* layer is not a shared *harness*, and if prompt assembly, tool mediation
and the agent loop are largely independent of the provider layer in dsh and Prime, then my
conclusion (c) is wrong and the grid was fine. I would rather have that corrected than agreed with.

## Correction, 2026-08-21: F5 did not survive pair review

Codex reviewed my inference rather than its own data, as asked, and returned **F5 DOES NOT SURVIVE
AS WRITTEN**. It was right on the two claims that mattered, and I verified its counter-evidence
before accepting it:

- **dsh is not a pi fork.** `dsh/packages/core/agent-loop/package.json` has **11 dependencies, none
  from pi or earendil-works** — all `@deepseek-ai/dsh-*`. dsh owns the agent loop, prompt assembly,
  tool mediation and context management. pi-ai sits *below* a dsh-owned adapter as provider
  transport. Nearly everything the bake-off wanted to vary sits **above** the shared layer.
- **Not one library.** dsh pins pi-ai 0.82.1, pi is 0.84.2, Prime ships its own fork at 0.7.4.
  Related code, not a shared dependency.
- **dsh has an independent cause.** Its adapter builds `Models` with no credential store and runs no
  login flow, so it rejects *every* OAuth-only provider — a boundary that survives pi-ai gaining
  MiniMax OAuth. So "one fact observed three times" was wrong.

The error worth keeping is the third one. **A confound must vary with the treatment.** A component
held *common* across cells reduces noise; it is the opposite of a confound. What I had was an
**external-validity limit** — pi and Prime are not independent architectures, so their auth results
are correlated evidence, not three ecosystem samples.

This project has spent weeks policing exactly that distinction, through arm M and through
tier-vs-family, and I inverted it the one time the error was mine and the conclusion was convenient.
"The experiment was confounded anyway" is a comfortable thing to believe on the day an experiment
dies for unrelated reasons, which is precisely when it should be checked hardest.

Corrected in F5 (rewritten and renamed), harness-bakeoff §12.1, harness-seams, the H5–H9 hypothesis
note, and the archived Phase 0 report's verification header. The seam report also lost three
overstatements codex caught independently: "byte-identical prompt channel" (no wire capture was
made), "dsh is the adoption target" (source-only probe), and "containment would have to come from
outside the harness" (too absolute — custom tools with `ipython` disabled were never evaluated).

**Phase 0's result is unchanged: 0 GO / 5 NO-GO, and the decision to stop did not rest on the claim
that was wrong.**
