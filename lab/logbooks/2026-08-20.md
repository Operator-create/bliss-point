# 2026-08-20 — day 2

**Settled D7.** Adopted the audit's positioning: task-contract compiler, headline is the refusal —
"it refuses to dispatch a brief the recipient cannot execute". Dials are the IR, linter is the
product. README, roadmap and repo description updated. Name and API unchanged.

**Pre-registered the 20-task A/B** on pi. Four information-matched arms built from one Task object
so only rendering differs. Wrote the power analysis honestly: n=20 binary resolves 25–30pp, so cost
and step metrics carry the finding and pass rate is directional only.

**Fixed a confound I built in.** "Tier A vs Tier B" conflated capability with vendor — an
interaction could have been one company's post-training preferring checklists wearing the costume of
a capability law. Now a 2×2 of {small, large} × {two families}: row effect means capability, column
effect means the profiles are vendor lore.

**Family bias.** Judging is a two-family panel with published inter-rater agreement; self-preference
is measured by having each judge score its own family blind, and if that bias exceeds the effect
being claimed, the judged portion is void. Objective metrics decide 16 of 20 tasks so judgement
touches as little as possible.

**Arm M died.** Sent it for audit suspecting it was weak. It was worse than weak — a straw man the
product could not lose to (refuted/R1). Replaced with a one-dial ablation across the render
threshold, scored on scope violations and steps. Published as Amendment 1 rather than a quiet
repair.

**Process failure worth recording.** A reply arrived at the expected path, in the expected format,
with file:line citations — and it was an audit of MacroGaijin, because the receiving pane had other
work queued and applied my "write your reply here" instruction to whatever it finished next. Every
quality signal was present and the answer was to the wrong question (findings/F1). Fixed by naming
reply files after the task, not the agent, and by checking the pane's title before trusting a reply.
The user then opened a dedicated grok session for this project.

**Got a real test machine.** `operatorserver` came back up and is on the tailnet, direct path, same
LAN. i7-7700HQ, **30 GiB RAM** against this laptop's 7.4 GiB, 8 GiB of working swap, 71 GB free,
Docker 29.6. Provisioned it: docker group, Node 22.22.1 pinned for pi, repo cloned, venv, and the
full suite passes on **Python 3.14** in 0.180s versus 0.877s here. Containers run as the user
without sudo.

This changes the eval plan for the better. "Fresh session per run" and "hidden tests the agent must
not read" are guarantees a container gives and discipline does not, and 480 runs can go unattended
instead of occupying the daily machine. The GTX 1050's 4 GB is not an inference host, so the eval's
models still come from APIs — the server's value is RAM, isolation and parallelism.

**Filed H4**, a handoff hypothesis. The observation that makes it belong here: Bliss Point already
compiles the outbound direction, and every profile already carries a `return_contract` describing
what must come back. A deterministic handoff renderer is the inverse of the compiler over the same
data, not a new subsystem. The prediction is a split — structured state templates cleanly,
unstructured reasoning does not — and the useful finding would be *which* fields need a model.

**Built the flat renderer (arm F)** before the task corpus, which was the point — a control written
after you know the tasks is a control you can tune.

The design question that mattered: arm F emits *everything*, while arm C *selects* — low
`context_volume` drops evidence, low `decomposition` collapses the subtask list. So the control
carries **more** text than the thing it controls for, and that is correct rather than a flaw. The
comparison is now honest in the direction that hurts: if a shaped brief wins while saying less,
shaping did the work; if it does not, gathering the fields was the whole product.

Enforced it with tests instead of intent: `flat()`'s signature is asserted to take only a task, so
recipient-awareness cannot creep in; every populated field must appear; and arm C's emitted fields
must be a subset of arm F's across all seven profiles and all six phases. Added a fourth test
asserting the compiler really does drop fields somewhere, because if it never did, the superset
property would be vacuously true and the whole comparison meaningless.

41 tests. A two-minute CLI hang mid-build turned out to be the external drive stalling, not code.
