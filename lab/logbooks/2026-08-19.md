# 2026-08-19 — day 1

**Started.** Repo scaffolded, MIT, pushed public: github.com/Operator-create/bliss-point.

**Decided.** Prompt compiler library, not a runtime (D0). Curated dial values first, eval bench as
phase 2. Seven dials resolved additively, no model in the compile path.

**Researched.** Cloned dsh, pi, pi-agent-harness rather than trusting press. dsh plugin seam is real
(confirmed/C1). pi's loop is 797 lines, not 300 — corrected a claim already published in our README
(confirmed/C2). pi-agent-harness assigns static model strings and does not vary shape by recipient,
so the differentiation holds.

**Caught a fabrication.** A "120× cost reduction" figure tagged FACT by an agent investigation
appears nowhere in the source tree. Removed, with a note in the docs saying why (findings/F1).

**Peer review, two families.** codex found the gap list was English strings that a CI gate and the
bench would both have to parse, and that a custom profiles directory could not actually compile —
`BLISSPOINT_PROFILES` replaced the whole directory while `resolve()` still demanded the modifier
tables. Both real, both fixed. It also argued *against* splitting `render()`, which I accepted.

grok attacked the seven-dial model as unevaluated taste and recommended merging to four. Built
`bliss correlate` instead of taking it on faith. One of its two proposed merges is supported
(+0.921), the other is refuted (−0.632, refuted/R2), and the tightest pair in the matrix was
predicted by nobody. Nothing merged: n=7, one author.

**Open.** Positioning — grok argues "prompt compiler" is the wrong hill and the linter is the
product.
